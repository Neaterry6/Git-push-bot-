{
  "name": "smart-terminal-bot",
  "version": "1.0.0",
  "description": "Smart memory-enabled Telegram terminal and coding assistant bot",
  "main": "bot.js",
  "scripts": {
    "start": "node bot.js",
    "check": "node --check src/bot.js && node --check src/tools.js && node --check src/utils/history.js && node --check src/utils/ai.js && node --check src/utils/claudePro.js && node --check src/utils/executor.js",
    "test": "npm run check"
  },
  "dependencies": {
    "adm-zip": "^0.5.16",
    "archiver": "^7.0.1",
    "axios": "^1.6.7",
    "dotenv": "^16.4.0",
    "fs-extra": "^11.3.2",
    "telegraf": "^4.16.3",
    "cheerio": "^1.0.0",
    "execa": "^9.5.2",
    "form-data": "^4.0.1",
    "playwright": "^1.49.1"
  }
}
